apiVersion: v1
kind: Service
metadata:
  name: timesheet-serv
  namespace: chap4
spec:
  selector:
    app: timesheet
  ports:
    - protocol: TCP
      port: 80         # External port
      targetPort: 8080 # Internal app port
  type: NodePort       
